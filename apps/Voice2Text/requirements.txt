# Voice2Text - dependencias del motor (lote 1, ADR-0001)
#
# faster-whisper arrastra ctranslate2, av (PyAV), onnxruntime, huggingface_hub y
# tokenizers como dependencias transitivas: no hace falta listarlas aparte.
# Sin tope de version: no hay ningun requisito de compatibilidad conocido todavia.
faster-whisper

# fetch.py (lote 4): enlaces de YouTube/TikTok/X/Facebook, aislado y opcional (D7).
# SIN techo de version: persigue cambios continuos de las plataformas (ADR-0001 Sec.9,
# "yt-dlp es una dependencia viva"). Se actualiza con:
#   py -3 -m pip install --upgrade yt-dlp
yt-dlp

# Se anade en un lote posterior, cuando el codigo que lo usa exista:
#   pywebview  (lote 3, app.py)
